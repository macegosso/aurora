export function Footer() {
  return (
    <footer className="relative z-10 border-t border-line">
      <div className="wrap flex flex-col gap-2 py-8 text-[13px] text-muted sm:flex-row sm:flex-wrap sm:justify-between sm:gap-3.5">
        <span>Aurora — case de produto · AI Product Challenge (Blip)</span>
        <span className="font-mono">case · protótipo · documentos</span>
      </div>
    </footer>
  );
}
